import { describe, it } from "node:test";
import * as assert from "node:assert";

describe("test add suit", () => {
  it("test add should work correctly", () => {
    assert.strictEqual(3, 1 + 2);
  });
});
